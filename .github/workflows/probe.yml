name: Probe artifact download
on:
  push:
    branches: [arena/**]
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "probe-ok" > probe.txt
      - uses: actions/upload-artifact@v4
        with:
          name: probe-artifact
          path: probe.txt
